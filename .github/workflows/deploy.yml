name: Deploy Nexora-Hug

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install API dependencies
        run: |
          cd api
          npm install
      
      - name: Install Client dependencies
        run: |
          cd client
          npm install
      
      - name: Build Client
        run: |
          cd client
          npm run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      
      - name: Deploy to Hugging Face
        run: |
          echo "Deploying Spaces to Hugging Face..."
          # TODO: Implementar deploy automático
        env:
          HF_TOKEN: ${{ secrets.HUGGINGFACE_TOKEN }}
      
      - name: Deploy API
        run: |
          echo "Deploying API..."
          # TODO: Implementar deploy a Railway/Render
      
      - name: Deploy Frontend
        run: |
          echo "Deploying Frontend..."
          # TODO: Implementar deploy a Vercel

